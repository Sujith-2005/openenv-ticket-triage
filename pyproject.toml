[project]
name = "customer-support-triage"
version = "0.1.0"
description = "Ticket Triage"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0",
]

[project.scripts]
server = "server.app:main"
